# PolicyProof starts in Deterministic Demo mode and needs no API key.
# Set this optional server-only value only when you explicitly choose a live GPT-5.6 feature.
# Never commit a real value. Live mode is never enabled by default.
OPENAI_API_KEY=
